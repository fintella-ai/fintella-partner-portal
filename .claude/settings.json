{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "//": "Claude Code project settings. Created per Day 1 BLOCK 8 of the 2-day launch plan. Most functional file exclusions come from .gitignore (Claude Code respects it by default); this file documents Claude-specific intent and collects future project-level configuration in one place.",
  "ignorePatterns": [
    "node_modules/**",
    ".next/**",
    "coverage/**",
    ".vercel/**",
    "out/**",
    "prisma/dev.db",
    "prisma/dev.db-journal"
  ],
  "permissions": {
    "//": "Beast mode — all tools auto-approved for this project. Destructive production actions (db wipe, force push to main) still require explicit John instruction per CLAUDE.md.",
    "allow": [
      "Bash(*)",
      "Read(*)",
      "Edit(*)",
      "Write(*)",
      "Glob(*)",
      "Grep(*)",
      "WebFetch(*)",
      "WebSearch(*)",
      "mcp__ide__executeCode(*)",
      "mcp__ide__getDiagnostics(*)"
    ],
    "deny": []
  }
}
